# TeleStyle 生图方法讲解

本文只说明当前唯一生图路径中的三个核心部分：

1. 南北 stereographic chart 的构造与双分支去噪；
2. 两张 decoded RGB chart 到 ERP 的重投影与赤道硬拼；
3. 赤道低频颜色匹配和残差梯度修复两级优化。

当前流程不使用 A1，也不在去噪过程中逐步交换南北分支的 latent。最终只得到一张 ERP。

## 1. 总体数据流

```text
输入 2:1 ERP
    │
    ├── 球面重投影 ──> North chart ──> North latent ──> 独立去噪 ──> VAE decode
    │                         ╲ 初始重叠带同步 ╱
    └── 球面重投影 ──> South chart ──> South latent ──> 独立去噪 ──> VAE decode
                                                                  │
                                              两张 decoded RGB chart
                                                                  │
                                             2× 超采样重投影到 ERP
                                                                  │
                                                  赤道 RGB 硬拼
                                                                  │
                                      第一级：赤道低频颜色对齐
                                                                  │
                                      第二级：赤道异常梯度修复
                                                                  │
                                      优化带外恢复原始硬拼像素
                                                                  │
                                                       最终 2:1 ERP
```

核心代码分布：

- `telestylepanorama_inference.py`：输入尺寸、chart 构造、参数传递和优化带外恢复；
- `telestyleimage_inference.py`：双分支推理、分别解码和优化调用顺序；
- `telestyle_spherical.py`：ERP 与 stereographic chart 之间的球面坐标映射；
- `training/geometry.py`：decoded RGB 重投影、硬拼和两级赤道优化。

## 2. 南北 chart 的构造

### 2.1 为什么使用两个 chart

ERP 在极区有很强的横向拉伸。当前方法不直接把整张 ERP 当作普通平面图送入模型，而是分别以北极和南极为中心建立两个方形 stereographic chart。这样每个极点位于对应 chart 的中心附近，极区结构不会先被 ERP 的经度展开严重拉长。

南北 chart 都跨过赤道一段距离。默认参数为：

```text
hemisphere_overlap_degrees = 15°
cap_degrees = 90° + overlap = 105°
```

因此两个 chart 在赤道附近观察到同一片球面区域，为初始 latent 对齐以及最终接缝修复提供共同内容。

### 2.2 坐标约定

所有球面变换都从像素中心出发，而不是把像素边界当作采样点。对于高度为 `H`、宽度为 `W` 的 ERP，第 `y, x` 个像素中心对应：

```text
latitude  = π/2 - π × (y + 0.5) / H
longitude = 2π × (x + 0.5) / W - π
```

再转换为单位球方向：

```text
direction = (
    cos(latitude) × sin(longitude),
    sin(latitude),
    cos(latitude) × cos(longitude)
)
```

经度采样采用 circular padding，因此 ERP 最左列和最右列在球面上连续。重采样使用双线性 `grid_sample`，并统一采用 `align_corners=False` 的像素中心语义。

### 2.3 stereographic chart

chart 的每个像素中心先映射到平面坐标 `(u, v)`，再通过 stereographic 逆投影得到单位球方向。北 chart 使用正纬度方向作为中心，南 chart 使用负纬度方向作为中心。

两个 chart：

- 尺寸完全相同；
- 必须为正方形；
- 边长必须能被 16 整除；
- 使用相同的 overlap；
- 从同一张 ERP 通过真实球面方向采样得到，而不是裁剪、旋转或 `torch.rot90` 得到。

默认 chart 边长是输入 ERP 高度对齐到 16 后的尺寸。风格参考图在进入两个分支前统一缩放到 `1024×1024`。

## 3. 双分支 latent 初始化与去噪

### 3.1 两份完整推理状态

North chart 和 South chart 分别调用一次 Qwen Image Edit 的输入准备流程。每个分支都包含：

- 自己的内容 chart；
- 同一张风格参考图；
- 同一个 prompt；
- 同一个 seed；
- 自己的 latent、edit latent 和条件状态。

两边使用相同 seed，是为了减少无意义的随机差异；但由于内容 chart 的球面视角不同，两份条件和后续预测并不相同。

### 3.2 只同步初始重叠带

模型开始去噪前，会构造 `HemisphereLatentProjector`，把两个 chart 的初始 latent 在赤道重叠带内映射到彼此坐标系。

同步过程先克隆两边输入，保证结果不依赖更新顺序：

```text
south_on_north = 把 South latent 重采样到 North chart
north_on_south = 把 North latent 重采样到 South chart
```

随后根据纬度生成南北置信度。默认 overlap 为 `15°` 时，权重在重叠带内从南向北平滑变化：

```text
north_weight = 0.5 + 0.5 × sin(clamp(latitude, -15°, 15°) × π / 30°)
```

North chart 在北侧更信任自己，South chart 在南侧更信任自己；重叠带外完全保留各自原 latent。同步后的 latent 同时写回各分支的 `noise` 字段，使两边从具有共同赤道结构的初始噪声状态开始。

这里需要特别注意：当前实现只在去噪开始前同步一次，不会在每个 denoising step 后再次融合 latent。

### 3.3 两条独立 scheduler 轨迹

North 分支使用 pipeline 创建的 scheduler，South 分支使用它的深拷贝。每个 denoising step 中执行：

```text
noise_pred_north = model(North 当前 latent, North 条件)
noise_pred_south = model(South 当前 latent, South 条件)

North latent = North scheduler.step(noise_pred_north)
South latent = South scheduler.step(noise_pred_south)
```

两个分支共享同一套模型权重，但 scheduler 状态和 latent 轨迹始终分开。不存在以下行为：

- 用 North latent 覆盖 South latent；
- 用 South latent 覆盖 North latent；
- A1 adapter 的跨分支特征注入；
- 每一步对赤道 latent 做强制平均。

这种设计保留了每个极区 chart 的独立生成能力，代价是最终赤道两侧可能存在颜色和纹理相位差，需要在 decoded RGB 空间修复。

## 4. 分别解码，而不是合成 ERP latent

去噪结束后，North latent 和 South latent 分别通过 VAE 解码：

```text
north_decoded = VAE.decode(North latent)
south_decoded = VAE.decode(South latent)
```

当前流程不会先把两个 chart 的 latent 非线性重投影并合成为 ERP latent。原因是 latent 中包含高频特征，直接对 latent 做球面拉伸、插值和融合容易破坏笔触与局部纹理。

因此球面重投影发生在 decoded RGB 张量上。RGB 空间的空间意义明确，更适合进行几何采样、抗锯齿和局部颜色修正。

## 5. decoded RGB 重投影与赤道硬拼

### 5.1 先建立 2× ERP 网格

目标 ERP 内部尺寸保持 2:1，并按 16 像素对齐。重投影时先建立目标高、宽各放大两倍的 ERP 网格：

```text
high_height = output_height × 2
high_width  = output_width × 2
```

高分辨率 ERP 的每个像素中心先转换为单位球方向，再分别映射到 North 和 South stereographic chart，得到：

```text
north_high = sample(north_decoded, north_grid)
south_high = sample(south_decoded, south_grid)
```

南半球最终使用 `south_yaw_degrees = 0°`，不额外旋转其经度方向。

### 5.2 赤道硬拼

高分辨率 ERP 按纬度直接决定像素所有者：

```text
latitude >= 0°  -> North
latitude <  0°  -> South
```

即：

```text
hard_high = where(latitude >= 0°, north_high, south_high)
```

这是严格的 RGB hard cut，没有跨赤道 alpha blend。硬拼保持了两侧 decoded RGB 的锐利纹理，不会因为宽范围羽化而把笔触变软。

### 5.3 area 抗锯齿缩小

North ERP、South ERP 和 hard-cut ERP 都从 2× 网格通过 `area` 插值缩小到目标尺寸。这样可减少 stereographic 到 ERP 非线性重采样造成的锯齿和单像素抖动。

重投影阶段还会计算默认 `±10°` 的 consistency mask，但当前最终两级赤道优化按各自的纬度参数直接控制作用范围，不依赖该 mask 决定最终像素。

## 6. 赤道第一级优化：低频颜色匹配

赤道硬拼最常见的问题是两张独立生成图在亮度、色温或大尺度颜色分布上略有差异。第一级只修正低频颜色，不直接平均高频纹理。

默认作用半宽：

```text
hemisphere_color_match_degrees = 6°
```

### 6.1 提取两侧低频分量

North ERP 和 South ERP 分别做高斯低通：

- 经度方向使用 circular padding，避免在 ERP 左右边界制造新接缝；
- 纬度方向使用 reflect padding；
- 默认模糊尺度为 `1°`，换算为像素后至少为 1 个像素。

得到：

```text
north_low = GaussianBlur(north_erp)
south_low = GaussianBlur(south_erp)
difference = south_low - north_low
```

### 6.2 对称地向中间颜色靠拢

在赤道北侧，对 hard cut 加上 `0.5 × difference`；在赤道南侧，减去 `0.5 × difference`：

```text
North correction = +0.5 × (south_low - north_low)
South correction = -0.5 × (south_low - north_low)
```

这相当于让两侧低频颜色对称地靠向共同中点，而不是把一侧完全匹配到另一侧，因而不会固定偏向 North 或 South。

修正强度由纬度 smoothstep 权重控制：

```text
normalized = clamp(1 - abs(latitude) / 6°, 0, 1)
weight = normalized² × (3 - 2 × normalized)
```

权重在赤道最大，在 `±6°` 边界平滑下降到 0。只有 `abs(latitude) < 6°` 的像素会改变，带外逐像素沿用原始 hard cut。

这一级解决的是低频颜色断层，不负责消除赤道边界处剩余的单行异常梯度。

## 7. 赤道第二级优化：异常梯度修复

完成低频颜色匹配后，赤道上下第一行之间仍可能出现窄而锐利的亮线、暗线或颜色跳变。第二级根据赤道两侧邻近行的自然变化趋势，估计并抵消异常梯度。

默认参数：

```text
hemisphere_seam_residual_degrees      = 2°
hemisphere_seam_residual_blur_degrees = 0.5°
```

### 7.1 测量实际与期望梯度

设赤道上方最后一行为 `N0`，其上一行为 `N1`；赤道下方第一行为 `S0`，其下一行为 `S1`。

实际跨赤道梯度为：

```text
actual_gradient = S0 - N0
```

期望梯度取两侧内部自然梯度的平均：

```text
expected_gradient = 0.5 × ((N0 - N1) + (S1 - S0))
```

多余的接缝梯度为：

```text
excess = actual_gradient - expected_gradient
```

如果 `excess` 很大，说明跨赤道跳变明显偏离南北两侧自身的局部变化趋势。

### 7.2 沿经度环形平滑残差

`excess` 是沿整条赤道的一维信号。代码使用 circular padding 对它做经度高斯平滑，默认角尺度为 `0.5°`：

```text
sigma_pixels = max(1, ERP_width × 0.5° / 360°)
```

环形平滑保证经度 `-180°` 和 `+180°` 连续，也避免优化赤道时引入新的左右接缝。

### 7.3 南北对称分摊修正量

赤道北侧加上 `0.5 × excess`，南侧减去 `0.5 × excess`。在赤道中心满权重时，新的跨赤道梯度变为：

```text
(S0 - 0.5 × excess) - (N0 + 0.5 × excess)
= actual_gradient - excess
= expected_gradient
```

因此这一步不是简单模糊赤道，而是把异常跨界梯度校正为邻近区域所暗示的自然梯度。

空间权重同样使用 smoothstep，在赤道最大，并在 `±2°` 边界平滑降为 0。带外像素保持第一级结果不变。

## 8. 最终带外恢复

两级优化本身都通过 `torch.where` 限制在各自赤道带内。内部对齐尺寸与原始输入尺寸不一致时，优化图和原始 hard-cut 图还会分别缩放回输入分辨率。

缩放插值可能让赤道优化的影响轻微扩散到边界外，因此最后执行一次明确的像素恢复：

```text
final_band = max(color_match_degrees, seam_residual_degrees)
```

只在 `abs(latitude) < final_band` 的行使用优化结果，其余行全部从原始 hard-cut ERP 复制。默认参数下，只有 `±6°` 内可能与原始硬拼图不同。

这一保护步骤使赤道优化具备清晰边界：

- 极区和中高纬度完全保持各自 decoded chart 的原始结果；
- 不会为了修复赤道而全局改色；
- 不会对整张 ERP 做模糊或全局融合。

## 9. 默认参数及职责

| 参数 | 默认值 | 作用 |
| --- | ---: | --- |
| `hemisphere_overlap_degrees` | `15°` | chart 越过赤道的范围，同时限定初始 latent 同步区域 |
| `consistency_degrees` | `10°` | 重投影阶段生成 consistency mask；当前两级优化不直接使用该 mask |
| `hemisphere_color_match_degrees` | `6°` | 第一级低频颜色匹配的半宽 |
| 低频匹配内部 `blur_degrees` | `1°` | 分离南北 ERP 低频颜色分量的高斯尺度 |
| `hemisphere_seam_residual_degrees` | `2°` | 第二级异常梯度修复的半宽 |
| `hemisphere_seam_residual_blur_degrees` | `0.5°` | 接缝残差沿经度平滑的角尺度 |
| `antialias_scale` | `2` | RGB chart 重投影的超采样倍率 |
| `south_yaw_degrees` | `0°` | 南 chart 最终重投影的经度偏移 |

参数约束：

- `0° < overlap < 45°`；
- `0° <= color_match_degrees <= overlap`；
- `0° <= seam_residual_degrees <= overlap`；
- residual 修复启用时，`seam_residual_blur_degrees` 必须为有限正数；
- 将颜色匹配或 residual 半宽设为 `0°`，可分别关闭对应一级优化。

## 10. 方法特性与边界

该方法的核心取舍是：

- 用极点居中的 chart 改善 ERP 极区形变；
- 用两条独立生成轨迹保留南北半球各自的结构与高频细节；
- 在 decoded RGB 空间做真实球面重投影，避免非线性变换 latent；
- 用硬拼保持锐度，再用局部、对称、低频优先的策略修复接缝；
- 严格限制修改纬度范围，不用全局调色掩盖局部问题。

它不能保证两个独立分支在赤道生成完全相同的物体语义。初始重叠带同步可以提供共同起点，两级 RGB 优化可以消除颜色断层和异常梯度，但如果南北两侧在赤道生成了明显不同的几何结构，局部颜色修正不会重新生成或语义对齐这些结构。
