# TeleStyle 南北双 Chart 联合风格迁移改进方案

## 1. 方案摘要

本方案将“球面几何能力”和“TeleStyle 风格迁移能力”解耦，分两个主阶段训练：

1. **A1：SphereAdapter 球面几何预训练**  
   使用大量普通、高质量、连续的 ERP，不使用风格参考图，不需要人工风格化 ERP 目标。在冻结大部分预训练 DiT 和 VAE 的前提下，训练 SphereAdapter，使模型学会南北双 chart 联合去噪、球面对应、全球信息交换、共享赤道状态和赤道硬裁切连续性。
2. **A.5：冻结 TeleStyle 教师的无真值风格兼容蒸馏**  
   加载并冻结现有 TeleStyle 能力，用原 TeleStyle 单 chart 推理作为教师，使用“普通 ERP + 非配对 style image + prompt”训练 SphereAdapter。在非重叠区保留教师的风格迁移行为，在赤道重叠区通过分别 VAE 解码后的 RGB、梯度、Laplacian 和感知一致性损失训练风格化高频连续性。

最终推理时：

```text
content ERP
  ↓
north content chart + south content chart
  ↓
共享的 Qwen/TeleStyle DiT + SphereAdapter 联合去噪
  ↓
north native latent + south native latent
  ↓                         ↓
frozen VAE decode          frozen VAE decode
  ↓                         ↓
north RGB chart            south RGB chart
  └────→ 抗混叠球面重投影 ←────┘
                ↓
       公共 ERP 网格赤道硬裁切
                ↓
          风格化无缝 ERP
```

本方案不再将南北 chart latent 非线性重投影为 ERP latent 后再解码，以避免当前路径中高频笔触抖动、锯齿和相位干涉。

---

## 2. 目标、非目标和成功条件

### 2.1 核心目标

- 南北两张 chart 在同一个逻辑 DiT forward 中联合生成，不是 batch 内两张相互独立的图。
- 两个分支保留各自原生 chart latent 轨迹，不把一个分支的整体 latent 强制回写给另一个分支。
- 模型学会每个 token 的二维 chart 位置和三维球面位置，能够识别重叠区中同一球面位置的南北 token。
- 分别解码后的南北 RGB chart 在公共 ERP 赤道带中同时满足低频、边缘和高频笔触一致性。
- 最终 ERP 不依赖宽羽化掩盖接缝，以赤道硬裁切作为主验收输出。
- 在增加球面能力时保留 TeleStyle 原有的风格理解、内容保真和笔触生成能力。

### 2.2 非目标

- A1 不负责学习新风格，也不负责替代 TeleStyle LoRA。
- A1 不追求从纯文本无条件生成任意 ERP；它首先是一个球面联合图像编辑/去噪能力。
- A.5 不依赖人工制作的风格化 ERP 真值；如果后续获得少量真值，可作为可选阶段 B 提高上限。
- 不通过强制南北 raw latent 在重投影后逐元素相等来实现一致性。
- 不修改或重训现有 VAE 作为第一版目标。

### 2.3 成功的定义

“无缝”分为三层：

1. **视觉无缝**：在 ERP 矩形图和球面 viewer 中都不能快速定位赤道裁切线。
2. **\(C^0\) 近似连续**：赤道两侧颜色、亮度和低频场没有异常跳变。
3. **\(C^1\) 近似连续**：跨赤道轮廓、法向梯度、笔触方向和纹理相位没有明显折断。

两次独立 VAE 解码不提供逐像素数学恒等保证，但可以通过真实重叠上下文、球面对应、解码后损失和硬接缝训练达到稳定的视觉无缝。

---

## 3. 模型拆分与参数归属

### 3.1 整体组成

```text
Base DiT
├── 原有单图 tokenization / patch embedding
├── 原有 chart-local Transformer blocks
├── 原有 text/content/style conditioning
└── 原有 output head

TeleStyle
└── 已训练的风格迁移 LoRA / adapters

SphereAdapter
├── chart ID embedding
├── spherical position projection
├── global sphere tokens
├── global cross-chart blocks
├── correspondence-aware attention
├── shared equator tokens
├── branch exchange / symmetry support
└── zero-initialized residual gates

Frozen VAE
├── encode target native charts
└── decode predicted native charts for RGB-domain losses
```

### 3.2 为什么 SphereAdapter 必须可插拔

- 便于分离验证球面能力和风格能力。
- 可以在不破坏单图 TeleStyle 的情况下单独训练。
- 便于做 `SphereAdapter on/off`、`cross-chart on/off`和 `shared-equator on/off` 消融。
- 可以分别保存球面几何权重和 TeleStyle 风格权重。
- 如果 A.5 训练失败，可以回退到 A1 几何检查点，而不需要回退基础模型。

### 3.3 零扰动初始化

新增的 cross-chart 和 shared-equator 路径使用 residual gate：

\[
h' = h + g\,R(h),
\]

其中 \(g\) 初始为 0 或很小的正数。这样初始模型近似原单图 DiT，避免新模块在训练开始时立即破坏预训练生成能力。

---

## 4. SphereAdapter 详细结构

### 4.1 双 token 流

南北 chart 分别 patchify，生成：

```text
H_N: [B, N, D]
H_S: [B, N, D]
```

它们属于同一个样本，但不是 batch 中两个独立样本。可以在模型内部保持两个 tensor，也可以在 sequence 维度打包，但 attention mask 和位置信息必须明确区分两张 chart。

### 4.2 双重位置信息

每个 token 同时拥有：

- 原有二维 chart 位置 \((u,v)\)，保留预训练 DiT 的平面图像先验；
- 单位球方向 \((x,y,z)\)，描述真实球面位置；
- chart ID，区分 north/south 局部坐标系；
- overlap flag 和到赤道的纬度距离；
- 可选的 local Jacobian/切平面方向信息。

第一版不应直接替换基础模型原有二维 RoPE，而应使用小幅球面 embedding 或 attention bias 增量注入，减少与预训练分布的冲突。

### 4.3 Chart-local blocks

大部分原 DiT block 分别对 \(H_N\) 和 \(H_S\) 执行，两边共享同一组权重：

```text
H_N → shared local block → H_N'
H_S → shared local block → H_S'
```

其职责是保留单 chart 内部的局部几何、全局构图和原有风格生成能力。

### 4.4 Global cross-chart blocks

每隔若干 local block 执行一次全球信息交换，用于：

- 全球构图；
- 主体数量和位置规划；
- 南北光照和色调统一；
- 风格强度和笔触尺度统一；
- 北极到南极的直接信息通路。

推荐先使用少量 `global_sphere_tokens`聚合信息，再广播回两个分支，以控制两倍 token 导致的 attention 开销。

### 4.5 Correspondence-aware attention

对赤道重叠带中的 token，使用球面角距离而不是 chart 二维距离建立邻接关系：

\[
\theta_{ij}=\arccos\left(\operatorname{clamp}(\mathbf d_i^\top\mathbf d_j,-1,1)\right).
\]

对应 attention logit 可写为：

\[
a_{ij}=\frac{q_i^\top k_j}{\sqrt d}+b_{sphere}(\theta_{ij})+b_{corr}(i,j).
\]

该模块主要负责高精度对齐跨赤道物体轮廓、细线、笔触方向和纹理相位，不代替全球语义 attention。

### 4.6 Shared-equator tokens

在公共球面坐标中定义一条小型 ERP 赤道 token 带：

```text
longitude: periodic
latitude:  [-consistency_degrees, +consistency_degrees]
```

每隔若干 block 执行：

```text
north overlap → shared equator
south overlap → shared equator
shared equator 内部循环经度 attention
shared equator → north overlap
shared equator → south overlap
```

Shared-equator token 是“同一球面状态”的可学习中介，不是对两个 raw latent 做 bilinear 平均。

### 4.7 推荐 block 排列

```text
2–4 个 chart-local blocks
→ 1 个 global cross-chart block
→ 1 个 correspondence/shared-equator block
→ 重复
```

第一版不必将所有 Transformer block 改造为完全联合 attention。

---

## 5. 球面数据与样本构造

### 5.1 A1 数据

每个基础样本只需要一张高质量连续 ERP：

```text
ERP I
├── north chart of I
└── south chart of I
```

基本要求：

- 真实等距柱状投影；
- 宽高比 2:1；
- 左右周期连续；
- 无明显拼接鬼影、曝光跳变或极区修补伪影；
- 原图分辨率不低于训练目标，理想为 4096×2048 或更高；
- 场景应包含室内、城市、自然、建筑、人物、道路和高频纹理。

### 5.2 A.5 数据

不需要风格化 ERP 真值，每个样本为：

```text
content ERP
+ unpaired style image
+ TeleStyle edit prompt
```

建议使用多对多随机配对：

```text
一张 ERP 对应多种 style
一种 style 对应多张 ERP
```

风格集覆盖：

- 油画、水彩、素描、版画、墨画；
- 密集短笔触、长笔触、颗粒、排线、装饰图案；
- 低对比和高对比风格；
- 大尺度色块和高频细纹风格。

不要让 style image 长期与固定 ERP 绑定，否则模型可能记忆内容对应而不是学习风格兼容。

### 5.3 高频压力数据

自然 ERP 外额外加入程序生成或人工挑选的压力样本：

- 穿过赤道的水平线、竖线、斜线和曲线；
- 棋盘格、条纹、编织纹理和砖墙；
- 多频率正弦信号；
- 小字、栏杆、线缆和细建筑边缘；
- 油画排线、铅笔排线和方向明显的笔触。

该部分数据用于训练后期提高采样权重、验证高频损失和做固定压力测试。

### 5.4 球面数据增强

在从 ERP 生成 chart 前对完整球面执行：

- 随机 yaw，消除固定经线偏差；
- 随机 pitch/roll，让天空、地面、建筑、人物和细线都有机会跨赤道；
- 随机南北交换；
- 随机 overlap，第一版可在 \(12^\circ\)–\(18^\circ\) 内采样；
- 同步颜色和压缩增强，不对南北 chart 做互不一致的几何增强。

球面旋转必须通过 ERP 像素中心、单位球坐标和重采样完成，不能用平面 `rot90` 代替。

### 5.5 数据划分

- 按原始 ERP `scene_id` 划分 train/validation/test。
- 同一 ERP 的不同球面旋转必须留在同一 split。
- 风格验证集同时保留“见过风格 + 未见 ERP”和“未见风格 + 未见 ERP”两类。
- 压力测试集固定 seed、旋转、overlap、prompt 和 style，便于跨 checkpoint 直接对比。

---

## 6. A1：SphereAdapter 球面几何预训练

### 6.1 训练输入和目标

对每张 ERP \(I\)，使用同一球面旋转和 overlap 构造：

\[
I_N=P_N(I),\qquad I_S=P_S(I).
\]

冻结 VAE encoder 产生原生 chart clean latent：

\[
z^N_0=E(I_N),\qquad z^S_0=E(I_S).
\]

采样同一 timestep \(t\)，为两个分支加噪：

\[
z^N_t=\alpha_t z^N_0+\sigma_t\epsilon_N,
\]

\[
z^S_t=\alpha_t z^S_0+\sigma_t\epsilon_S.
\]

第一版可使用独立 \(\epsilon_N,\epsilon_S\)，但必须由同一用户 seed 确定性派生，便于复现。后续可消融 shared-equator noise，但不在第一版中强制使用 bilinear warp 的共享白噪声。

### 6.2 参数冻结表

| 模块 | A1 状态 |
|---|---|
| VAE encoder/decoder | 冻结，但 decoder 保留对输入 latent 的梯度 |
| Base DiT 主干 | 初期冻结 |
| 原有 content/text conditioning | 冻结 |
| TeleStyle LoRA | 关闭，不参与 A1 |
| Chart ID / spherical position | 训练 |
| Global cross-chart blocks | 训练 |
| Correspondence blocks | 训练 |
| Shared-equator tokens | 训练 |
| Residual gates | 训练 |
| 可选 DiT LoRA | 仅在 SphereAdapter 容量不足时低学习率训练 |

### 6.3 基础 diffusion loss

根据基础 DiT 的预测参数化分别计算：

\[
\mathcal L_{diff}
=
\mathcal L^N_{diff}
+
\mathcal L^S_{diff}.
\]

这一项的首要作用是保证南北两张 chart 分别解码时仍是正常高质量图像，不能为了接缝一致性牺牲单 chart 质量。

### 6.4 解码后公共球面损失

对模型预测的 clean latent \(\hat z^N_0,\hat z^S_0\) 分别解码：

\[
\hat I_N=D(\hat z^N_0),\qquad \hat I_S=D(\hat z^S_0).
\]

将它们重投影到完全相同的 ERP 像素中心网格：

\[
E_N=R_N(\hat I_N),\qquad E_S=R_S(\hat I_S).
\]

在主一致性 mask \(M_C\) 中计算：

#### RGB/Charbonnier

\[
\mathcal L_{rgb}
=
\frac{\sum M_C\sqrt{(E_N-E_S)^2+\varepsilon^2}}{\sum M_C}.
\]

#### 梯度一致性

\[
\mathcal L_{grad}
=
\|M_C\odot(\nabla E_N-\nabla E_S)\|_1.
\]

#### 多尺度 Laplacian

\[
\mathcal L_{lap}
=
\sum_k w_k
\|M_{C,k}\odot(L_k(E_N)-L_k(E_S))\|_1.
\]

该损失重点约束细线、笔触、重复纹理和高频相位。

#### 感知一致性

\[
\mathcal L_{perc}
=
\sum_l
\|M_{C,l}\odot(\Phi_l(E_N)-\Phi_l(E_S))\|_1.
\]

感知损失不应单独使用，因为它可能对小相位偏移不敏感。

### 6.5 硬接缝损失

使用与推理一致的硬裁切：

\[
E_H(\lambda,\varphi)=
\begin{cases}
E_N(\lambda,\varphi),&\varphi\ge0,\\
E_S(\lambda,\varphi),&\varphi<0.
\end{cases}
\]

在赤道上下若干行直接计算法向一阶和二阶差分异常：

\[
\mathcal L_{seam}
=
\mathcal L_{normal-gradient}
+
\lambda_{second}\mathcal L_{normal-second-difference}.
\]

这项损失必须作用于硬接缝输出，不能作用于已经宽羽化的图像。

### 6.6 防模糊损失

如果只优化 RGB 一致性，模型容易把赤道带变平滑。增加 overlap 和附近非 overlap 区域的高频能量比值约束：

\[
\mathcal L_{energy}
=
\left|
\frac{HF(E_H,M_C)}{HF(E_H,M_{near})+epsilon}-1
\right|.
\]

该项只防止赤道整体过度变糊，不强制两个不同内容区域具有完全相同的频谱。

### 6.7 对称性损失

对一定比例样本执行南北交换和球面上下翻转，要求交换前后的预测等变：

\[
\mathcal L_{sym}
=
\|p_N'-T(p_S)\|
+
\|p_S'-T(p_N)\|.
\]

### 6.8 防止模型忽略跨 chart 模块

单纯使用完整的两张 chart 重建原图，模型可能只依赖各自分支，而忽略 cross-chart attention。需要加入：

- **Content-condition dropout**：随机遮挡一个分支重叠区的内容条件，但保留另一个分支。
- **Asymmetric corruption**：对一个分支的少量 overlap token 施加可记录的额外扰动，令其从另一分支恢复对应信息。
- **Cross-chart dropout**：少量 batch 关闭跨 chart 路径，用于构造明确消融基线，不是作为主训练路径。
- **Global-token bottleneck task**：对一个分支的全局条件做低概率 dropout，迫使它使用 global sphere tokens 获得另一半球的全局信息。

扰动不能过强，否则任务会从球面联合去噪变成大面积修复。

### 6.9 A1 总损失

\[
\begin{aligned}
\mathcal L_{A1}={}&
\mathcal L_{diff}
+\lambda_{rgb}\mathcal L_{rgb}
+\lambda_{grad}\mathcal L_{grad}\\
&+\lambda_{lap}\mathcal L_{lap}
+\lambda_{perc}\mathcal L_{perc}
+\lambda_{seam}\mathcal L_{seam}\\
&+\lambda_{energy}\mathcal L_{energy}
+\lambda_{sym}\mathcal L_{sym}.
\end{aligned}
\]

不建议从第一步就打开所有解码损失或给它们较大权重。

---

## 7. A1 训练 Curriculum

### 7.1 A1-Warmup：保护单 chart 能力

目标：让新增 residual gate 从 0 稳定打开，不立即改变原 DiT 行为。

- 冻结 Base DiT 和 VAE。
- 只训练 SphereAdapter。
- 以 diffusion loss 为主。
- 少量使用 RGB consistency，暂不使用强 Laplacian 和 hard-seam loss。
- 监控单 chart 的扩散损失和解码质量不得明显劣化。

### 7.2 A1-Geometry：对应和全局信息

目标：打通南北全局和对应信息。

- 逐渐提高 cross-chart 和 shared-equator gate。
- 增加随机球面旋转、南北交换和 overlap 随机化。
- 加入 content-condition dropout 和小范围 asymmetric corruption。
- 开启 RGB/gradient consistency。
- 定期使用 hard-cut ERP 验证。

### 7.3 A1-HighFreq：高频接缝

目标：不通过模糊赤道换取一致性。

- 提高高频压力样本采样率。
- 开启多尺度 Laplacian、hard-seam 和 high-frequency energy loss。
- 提高低噪声 timestep 上的解码损失采样率，因为预测 \(x_0\) 在该区域更稳定，且后期去噪更直接决定笔触。
- 同时监控 overlap 和 nearby 区域的高频能量比。

### 7.4 A1 通过条件

进入 A.5 前至少满足：

- 南北 chart 分别解码质量不显著低于原单图模型。
- 赤道硬裁切在自然 ERP 上达到稳定视觉无缝。
- 赤道带没有系统性模糊。
- 关闭 correspondence/shared-equator 后接缝指标明显变差，证明模块真正被使用。
- 随机经度旋转、南北交换和不同 overlap 下结果稳定。

---

## 8. A.5：TeleStyle 教师兼容蒸馏

### 8.1 目的

A1 学到的球面几何在很大程度上与风格无关，但风格化高频并非完全与风格无关。油画笔触、素描排线、版画颗粒和周期装饰纹理会改变赤道一致性的频率分布和相位敏感性。

A.5 的任务是：

```text
保留 TeleStyle 教师的风格迁移行为
+
让风格化南北输出在赤道公共球面带中一致
```

### 8.2 教师和学生

#### 教师

原始、冻结的 TeleStyle 单图模型：

```text
teacher_north = TeleStyle(content_north, style, prompt)
teacher_south = TeleStyle(content_south, style, prompt)
```

教师南北两次 forward 相互独立，因此教师可以保证单 chart 风格质量，但不是赤道一致性真值。

#### 学生

加载相同 Base DiT 和 TeleStyle 权重，再加载 A1 SphereAdapter：

```text
student_north, student_south
    = JointTeleStyleSphereAdapter(
        content_north,
        content_south,
        style,
        prompt,
        geometry,
      )
```

学生是同一个联合 forward，教师是两个独立 forward。

### 8.3 同步噪声、timestep 和条件

蒸馏必须使教师和学生在可比状态下预测：

- 使用相同 timestep \(t\)；
- 对同一 chart 使用相同 noisy latent；
- 使用相同 content chart；
- 使用相同 style image；
- 使用相同 prompt、CFG 和 conditioning 参数。

否则 teacher/student prediction 差中会混入与 SphereAdapter 无关的噪声和条件差异。

### 8.4 空间分区

定义三个区域：

```text
Core region:
    一个 chart 明确拥有最终输出权的主体区域

Consistency region:
    例如 |φ| <= 10°，南北都必须表示同一风格化球面场

Guard region:
    主一致性带到 chart 外边界之间的真实上下文区域
```

教师蒸馏在 core region 权重最强，在 consistency region 保留较弱权重；学生南北一致性损失只在 consistency region 内主要计算。

### 8.5 教师预测蒸馏

设教师预测为 \(p^T_N,p^T_S\)，学生预测为 \(p^S_N,p^S_S\)。用 core mask \(M_{core}\) 计算：

\[
\mathcal L_{teacher-core}
=
\|M^N_{core}\odot(p^S_N-\operatorname{sg}(p^T_N))\|_1
+
\|M^S_{core}\odot(p^S_S-\operatorname{sg}(p^T_S))\|_1.
\]

在 consistency region 只保留弱教师锚定：

\[
\mathcal L_{teacher-overlap}
=
\|M_C\odot(p^S_N-\operatorname{sg}(p^T_N))\|_1
+
\|M_C\odot(p^S_S-\operatorname{sg}(p^T_S))\|_1.
\]

其权重应明显低于 core 蒸馏。原因是教师的南北赤道细节可能互相矛盾，不能同时把两者当作强真值。

### 8.6 解码后风格化球面一致性

从学生预测得到 \(\hat z^S_{0,N},\hat z^S_{0,S}\)，分别解码并重投影：

\[
E^S_N=R_N(D(\hat z^S_{0,N})),
\]

\[
E^S_S=R_S(D(\hat z^S_{0,S})).
\]

在 \(M_C\) 中计算：

\[
\mathcal L_{style-rgb}
+
\mathcal L_{style-grad}
+
\mathcal L_{style-lap}
+
\mathcal L_{style-perc}.
\]

这组损失不要对教师的两个重叠输出做平均后当作目标，而是直接要求学生两个分支在公共球面网格中相互一致。

### 8.7 风格保持损失

为防止学生为了赤道一致性减弱风格，比较教师和学生的风格特征：

- 感知特征均值和方差；
- Gram matrix；
- 可选的 style/CLIP embedding；
- 多尺度频谱能量；
- 笔触尺度和方向统计。

\[
\mathcal L_{style-preserve}
=
d(\Psi(E^S),\operatorname{sg}(\Psi(E^T))).
\]

强风格约束主要作用在 core region 和赤道附近的所有权区域，但不应把南北教师的具体高频相位同时强制复制到学生。

### 8.8 内容保真

使用教师输出特征、原 content 的结构特征或现有 TeleStyle 内容保真目标，防止 SphereAdapter 为了联合一致性改变物体几何。内容损失应优先对边缘、深层感知特征和低频结构施加，而不是强迫风格化输出逐像素接近 content RGB。

### 8.9 A.5 总损失

\[
\begin{aligned}
\mathcal L_{A.5}={}&
\lambda_{tc}\mathcal L_{teacher-core}
+\lambda_{to}\mathcal L_{teacher-overlap}\\
&+\lambda_{rgb}\mathcal L_{style-rgb}
+\lambda_{grad}\mathcal L_{style-grad}
+\lambda_{lap}\mathcal L_{style-lap}\\
&+\lambda_{perc}\mathcal L_{style-perc}
+\lambda_{sp}\mathcal L_{style-preserve}
+\lambda_{content}\mathcal L_{content}\\
&+\lambda_{seam}\mathcal L_{style-seam}
+\lambda_{energy}\mathcal L_{style-energy}.
\end{aligned}
\]

权重关系的原则为：

```text
core region:
    teacher preservation 强

consistency region:
    student north/south consistency 强
    teacher preservation 弱

all valid regions:
    content structure and style statistics 保持
```

---

## 9. A.5 参数冻结和 Curriculum

### 9.1 参数冻结表

| 模块 | A.5 状态 |
|---|---|
| Teacher Base DiT | 冻结，`no_grad` |
| Teacher TeleStyle LoRA | 冻结，`no_grad` |
| Student Base DiT | 初期冻结 |
| Student TeleStyle LoRA | 加载并冻结 |
| Student SphereAdapter | 训练 |
| VAE | 冻结参数，学生解码路径保留输入梯度 |
| 可选 Student DiT LoRA | 第二轮才以低学习率解冻 |
| 可选 TeleStyle LoRA 副本 | 只在明确证明风格与球面模块不兼容时微调 |

### 9.2 A.5-Warmup

- 只使用教师 core distillation 和较弱的 overlap RGB/gradient consistency。
- 保持 A1 gate 的学习率较小，防止几何能力被快速覆盖。
- 以低频和中频风格为主，先让色调和风格强度稳定。

### 9.3 A.5-StyleConsistency

- 扩大风格种类和强度。
- 开启 Laplacian、perceptual、hard-seam 和 high-frequency energy loss。
- 提高油画排线、素描和重复装饰纹理样本的采样率。
- 逐渐降低 consistency region 的 teacher prediction 权重，让学生有空间解决两个教师分支的相位冲突。

### 9.4 A.5-Stabilization

- 使用真实推理步数、CFG、LoRA 强度和 chart 分辨率训练/验证。
- 对未见 style 和未见 ERP 做独立验证。
- 如果 SphereAdapter 容量确实不足，仅解冻少量靠近 cross-chart 注入点的 Student DiT LoRA，学习率低于 SphereAdapter。
- 任何解冻都必须同时运行单图 TeleStyle 回归测试。

---

## 10. Teacher–Student 计算与训练伪代码

### 10.1 A1 伪代码

```python
erp = sample_erp()
rotation = sample_spherical_rotation()
overlap = sample_overlap_degrees()

erp = rotate_erp_on_sphere(erp, rotation)
content_n, content_s, geometry = make_hemisphere_charts(erp, overlap)

with torch.no_grad():
    z0_n = vae.encode(content_n)
    z0_s = vae.encode(content_s)

t = sample_shared_timestep()
noise_n, noise_s = sample_branch_noises()
zt_n = add_noise(z0_n, noise_n, t)
zt_s = add_noise(z0_s, noise_s, t)

pred_n, pred_s = student_dit(
    zt_n, zt_s,
    content_n, content_s,
    geometry=geometry,
    timestep=t,
    sphere_adapter=True,
    telestyle=False,
)

loss = diffusion_loss(pred_n, noise_n)
loss += diffusion_loss(pred_s, noise_s)

if should_compute_decode_loss(t):
    x0_n = predict_x0(zt_n, pred_n, t)
    x0_s = predict_x0(zt_s, pred_s, t)
    rgb_n = vae.decode(x0_n)  # VAE params frozen, input gradient retained
    rgb_s = vae.decode(x0_s)
    erp_n, erp_s = reproject_to_shared_erp(rgb_n, rgb_s, geometry)
    loss += geometric_overlap_losses(erp_n, erp_s, geometry)
    loss += hard_seam_loss(erp_n, erp_s, geometry)

loss.backward()
optimizer.step()
```

### 10.2 A.5 伪代码

```python
erp, style, prompt = sample_unpaired_style_batch()
content_n, content_s, geometry = make_augmented_charts(erp)

with torch.no_grad():
    z0_n = vae.encode(content_n)
    z0_s = vae.encode(content_s)

t = sample_shared_timestep()
noise_n, noise_s = sample_branch_noises()
zt_n = add_noise(z0_n, noise_n, t)
zt_s = add_noise(z0_s, noise_s, t)

with torch.no_grad():
    teacher_n = telestyle_teacher(
        zt_n, content_n, style, prompt, timestep=t
    )
    teacher_s = telestyle_teacher(
        zt_s, content_s, style, prompt, timestep=t
    )

student_n, student_s = joint_student(
    zt_n, zt_s,
    content_n, content_s,
    style, prompt,
    geometry=geometry,
    timestep=t,
)

loss = teacher_core_distillation(
    student_n, student_s,
    teacher_n, teacher_s,
    geometry.core_masks,
)
loss += weak_teacher_overlap_anchor(
    student_n, student_s,
    teacher_n, teacher_s,
    geometry.consistency_masks,
)

if should_compute_decode_loss(t):
    x0_n = predict_x0(zt_n, student_n, t)
    x0_s = predict_x0(zt_s, student_s, t)
    rgb_n = vae.decode(x0_n)
    rgb_s = vae.decode(x0_s)
    erp_n, erp_s = reproject_to_shared_erp(rgb_n, rgb_s, geometry)
    loss += styled_overlap_losses(erp_n, erp_s, geometry)
    loss += hard_seam_loss(erp_n, erp_s, geometry)
    loss += style_preservation_loss(
        erp_n, erp_s, teacher_n, teacher_s, style
    )

loss.backward()
optimizer.step()
```

### 10.3 计算量注意事项

A.5 每个 batch 概念上需要：

```text
2 个 teacher single-chart forward（no_grad）
+ 1 个 student joint forward
+ 可选的 2 个 native-chart VAE decode
```

可以通过以下方式减少成本：

- 解码后损失只在一定比例 batch 上执行；
- 优先在低噪声 timestep 执行完整 VAE 损失；
- teacher 使用 `no_grad` 和更低精度，但不得改变 teacher/student 的条件和 timestep；
- 可离线缓存固定样本、固定 timestep 和固定噪声的 teacher 压力测输出，但主训练不应只依赖固定缓存；
- 使用 gradient checkpointing、Flash Attention 和 mixed precision；
- 如果局部 VAE ROI decode 经过专门验证，可对赤道 ROI 附加充足 halo 后解码，否则保留完整 chart decode。

---

## 11. 学习率、权重和训练稳定性

### 11.1 参数组

建议至少划分：

```text
group_sphere_position
group_cross_chart
group_correspondence
group_shared_tokens
group_residual_gates
group_optional_dit_lora
```

原则：

- 新增小型 embedding/token 可使用相对较高学习率；
- cross-attention projection 使用中等学习率；
- residual gate 使用小学习率并监控幅度；
- 任何 Base DiT/TeleStyle LoRA 解冻参数都使用明显更小学习率；
- 不使用一个统一高学习率同时更新新增模块和预训练权重。

### 11.2 损失权重 Curriculum

权重调度遵循：

```text
先保证每张 chart 是好图
→ 再保证低频和轮廓一致
→ 最后加强高频相位和硬接缝
```

当出现赤道模糊时，不要继续提高 RGB consistency，而应：

- 降低 RGB loss；
- 检查 raw latent 是否被过强同步；
- 提高 Laplacian、style preservation 和 high-frequency energy 损失；
- 提高高频压力样本的比例。

### 11.3 EMA 和检查点

- 保存 SphereAdapter EMA 权重。
- A1 和 A.5 使用不同 checkpoint 命名空间。
- 每个 checkpoint 记录 Base DiT hash、TeleStyle LoRA hash、VAE hash、projection config 和 overlap config。
- 不将 SphereAdapter 直接合并进 Base DiT 作为唯一保存形式，保留可拆卸权重。

---

## 12. RGB 重投影和最终推理

### 12.1 原生 chart 解码

去噪完成后：

```text
rgb_north = VAE.decode(latent_north)
rgb_south = VAE.decode(latent_south)
```

可以把两张 latent 在 batch 维度并行解码，但 batch 并行不代表两张图在 VAE 内部交换特征。接缝连续性必须在 DiT 联合生成和训练损失中建立。

### 12.2 公共 ERP 像素网格

南北 RGB 必须重投影到完全相同的：

- ERP 输出分辨率；
- 像素中心定义；
- 经纬度方向；
- 颜色空间；
- 重采样滤波器；
- 经度循环规则。

不允许一个分支使用不同的半像素约定或另一套 `align_corners` 设置。

### 12.3 抗混叠采样

立体投影到 ERP 存在位置相关缩放。当局部发生缩小时，普通 bilinear 不是完整的抗混叠滤波器。推荐根据局部 Jacobian 使用：

- mipmap + trilinear；
- EWA 各向异性滤波；
- 或其它 footprint-aware 重采样。

否则模型生成已经正常时，最后 RGB 投影仍可以独立产生锯齿。

### 12.4 赤道硬裁切

\[
E(\lambda,\varphi)=
\begin{cases}
E_N(\lambda,\varphi), & \varphi\ge0,\\
E_S(\lambda,\varphi), & \varphi<0.
\end{cases}
\]

训练验收不使用宽羽化。产品模式如果需要安全保护，可在极窄区域中只校正低频色差，高频始终使用单一分支，不对高频笔触做宽带平均。

---

## 13. 评估体系

### 13.1 单 chart 质量

分别保存南北 RGB chart 并计算/检查：

- diffusion validation loss；
- reconstruction/perceptual quality；
- 清晰度和高频能量；
- 内容保真；
- A.5 的风格强度和教师特征相似度；
- chart 外边界附近的 padding 伪影。

### 13.2 公共赤道带

对 \(E_N,E_S\) 计算：

- MAE/Charbonnier；
- PSNR/SSIM；
- LPIPS；
- gradient MAE；
- Laplacian pyramid 各频带误差；
- 局部 phase correlation 峰值偏移；
- overlap/nearby 高频能量比。

### 13.3 硬接缝

- 赤道上下行颜色跳变；
- 纬度方向一阶差分异常；
- 纬度方向二阶差分异常；
- 细线跨赤道断裂率；
- 人工盲测接缝可见率；
- 球面 viewer 中多 FOV、多视角检查。

### 13.4 TeleStyle 能力保留

将联合学生与冻结教师比较：

- core region 的风格特征相似度；
- 内容结构保真；
- 风格强度；
- 未见 style 泛化；
- 单图输入的 TeleStyle 回归质量；
- 是否因为赤道一致性导致整体风格变弱。

### 13.5 起始验收门槛

数值门槛应在建立 baseline 后根据数据分布设定。第一版可使用相对门槛：

- 单 chart 质量不低于冻结教师或原基线的可接受范围；
- 赤道硬接缝梯度异常显著低于独立双分支基线；
- overlap 高频能量不应因训练系统性下降；
- 关闭 SphereAdapter 后球面指标明显退化；
- 加载 SphereAdapter 后 TeleStyle core region 风格特征不应显著退化。

---

## 14. 必做的消融实验

### 14.1 A1 消融

1. 两个独立分支，无 SphereAdapter。
2. 只共享 Base DiT 权重，无 cross-chart 信息交换。
3. 只有 global cross-chart attention。
4. Global cross-chart + spherical correspondence。
5. Global cross-chart + correspondence + shared-equator tokens。
6. 无 asymmetric corruption/content-condition dropout。
7. 具有上述防忽略任务。
8. 只有 RGB consistency。
9. RGB + gradient + Laplacian + energy。
10. \(10^\circ\)、\(15^\circ\)、\(20^\circ\) overlap。

### 14.2 A.5 消融

1. A1 SphereAdapter 直接加载 TeleStyle，不做 A.5。
2. 只做 teacher core distillation。
3. Teacher core distillation + RGB consistency。
4. 加入 gradient/Laplacian consistency。
5. 加入 style preservation。
6. Consistency region 使用强 teacher anchor 与弱 teacher anchor 对比。
7. TeleStyle LoRA 冻结与微调对比。
8. 独立噪声与 shared-equator noise 对比。
9. Bilinear RGB 重投影与抗混叠重投影对比。

所有消融同时报告单 chart 质量、赤道硬接缝、高频能量、风格保真、显存和速度，不允许只报告接缝误差。

---

## 15. 失败模式和回退策略

### 15.1 A1 单 chart 质量下降

可能原因：

- residual gate 打开过快；
- cross-chart 模块幅度过大；
- 过早解冻 Base DiT；
- RGB consistency 权重过大。

回退：重新冻结 Base DiT，降低 gate 和一致性权重，延长 diffusion-only warmup。

### 15.2 赤道连续但模糊

可能原因：

- RGB L1/Charbonnier 过强；
- 对 raw latent 做了过强的跨投影平均；
- 缺少 Laplacian 和 high-frequency energy 监控；
- 高频样本不足。

回退：降低低频一致性权重，禁用 raw-latent 强同步，增加高频损失和压力数据。

### 15.3 A.5 后风格变弱

可能原因：

- consistency region 的 teacher anchor 过弱；
- style preservation loss 不足；
- 学生通过降低笔触能量来缩小南北差异；
- SphereAdapter 对整个 chart 注入过强。

回退：提高 core teacher/style preservation，约束 gate，将精确一致性主要集中在赤道带。

### 15.4 风格正常，赤道笔触仍抖动

优先排查：

1. 南北 RGB 是否使用同一 ERP 像素中心；
2. RGB 重投影是否缺少抗混叠；
3. correspondence grid 是否存在半像素或方向错误；
4. Laplacian/gradient 损失是否只在低分辨率计算；
5. teacher overlap anchor 是否过强，强迫学生同时复制两套互相矛盾的高频。

### 15.5 模型忽略 cross-chart

表现：关闭 SphereAdapter 后结果几乎不变。

回退：增加跨赤道物体样本、content-condition dropout、小范围 asymmetric corruption 和共享 token 容量，并检查 residual gate 是否始终接近 0。

---

## 16. 实施顺序

### 里程碑 0：无训练渲染基线

1. 保存现有南北 latent 分别解码的 RGB chart。
2. 实现两张 RGB chart 到公共 ERP 网格的重投影。
3. 实现赤道硬裁切。
4. 实现差分图、gradient、Laplacian 和高频能量指标。
5. 验证 bilinear 与抗混叠重投影的差异。

如果这一阶段的几何和重采样本身存在错误，不得用后续训练来掩盖。

### 里程碑 1：SphereAdapter 最小原型

1. 双 token 流和 chart ID。
2. 球面位置 embedding。
3. 一个 global cross-chart block。
4. 一个 correspondence block。
5. 零初始化 residual gate。
6. 仅 diffusion loss 的 A1-Warmup。

### 里程碑 2：A1 完整几何训练

1. Shared-equator tokens。
2. 解码后 RGB/gradient consistency。
3. 硬接缝 loss。
4. 高频压力数据和 Laplacian/energy loss。
5. 南北对称与防忽略任务。
6. A1 完整消融。

### 里程碑 3：A.5 教师蒸馏

1. 固定 teacher/student 同噪声、同 timestep 预测对比。
2. Core/consistency/guard mask。
3. Core teacher distillation。
4. 学生赤道 RGB/gradient consistency。
5. Style preservation 和 content preservation。
6. 高频风格压力测试。

### 里程碑 4：少量真值可选微调

如果获得少量“content ERP + style + 全球连续 target stylized ERP”，可以在 A.5 后加入阶段 B，直接对目标南北 chart 的 diffusion target 和 target ERP 解码损失微调。该阶段提高上限，但不是 A1/A.5 启动的前置条件。

---

## 17. 文件和配置建议

后续实现时建议保持推理代码和训练代码边界清晰，示例拆分：

```text
training/
├── datasets/
│   ├── erp_geometry_dataset.py
│   └── unpaired_style_dataset.py
├── models/
│   ├── sphere_adapter.py
│   ├── joint_chart_dit.py
│   └── shared_equator.py
├── losses/
│   ├── spherical_overlap.py
│   ├── hard_seam.py
│   └── style_distillation.py
├── train_sphere_adapter.py
├── train_style_compatibility.py
└── evaluate_hard_seam.py
```

训练配置至少包含：

```yaml
geometry:
  chart_projection: stereographic
  overlap_degrees_range: [12, 18]
  consistency_degrees: 10
  erp_pixel_centers: true
  circular_longitude: true

model:
  use_global_sphere_tokens: true
  use_correspondence_attention: true
  use_shared_equator_tokens: true
  sphere_adapter_zero_init: true

training:
  stage: A1  # A1 or A.5
  freeze_base_dit: true
  freeze_vae: true
  freeze_telestyle: true
  decode_loss_probability: 0.25

inference:
  decode_native_charts: true
  latent_erp_composition: false
  rgb_reprojection_antialias: true
  equator_hard_cut: true
```

上述数值是起点，不是未经实验即固定的最终超参。

---

## 18. 最终决策标准

本方案应在同时满足以下条件后进入正式推理替换：

1. A1 在无风格 ERP 上能够通过赤道硬裁切验收，且不依赖宽羽化。
2. A1 南北原生 chart 分别解码清晰，赤道带没有被系统性低通。
3. A.5 学生在 core region 保持冻结 TeleStyle 教师的风格和内容行为。
4. A.5 在油画、排线、周期纹理等高频风格上仍能赤道硬裁切无缝。
5. RGB 重投影本身通过高频抗混叠压力测试，不把重采样锯齿错误归因给模型。
6. 关闭 global cross-chart、correspondence 或 shared-equator 后相应指标显著退化，证明联合能力不是后处理假象。
7. 在未见 ERP、未见 style、不同 overlap、不同分辨率和随机球面旋转下保持稳定。

最终系统应被理解为：

> TeleStyle 仍然负责“怎么风格化”，SphereAdapter 负责“怎么把南北两张 chart 作为同一个球面联合生成”。A1 先用普通 ERP 学会球面几何，A.5 再用冻结 TeleStyle 教师和解码后球面一致性损失学会风格化高频兼容。最终两张 chart 在原生坐标中分别解码，在公共 ERP 网格上裁掉重叠区并硬拼接，而不再依赖非线性 latent warp 或宽羽化隐藏接缝。
